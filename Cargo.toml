[package]
name = "cuda-sandbox"
version = "0.1.0"
edition = "2021"
description = "Sandbox — execution isolation, resource limits, fault containment, and safe experimentation for agents"

[dependencies]
serde = { version = "1", features = ["derive"] }
